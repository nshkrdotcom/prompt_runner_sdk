You are running in the `alpha/` repository.
The sibling `beta/` repository is available at `../beta/`.

Use Bash commands (not the Write tool) to do the following:
1) In the current repo, create or update `NOTES.md` with exactly one line: "alpha ok".
2) In the sibling repo, create or update `../beta/NOTES.md` with exactly one line: "beta ok".

Write directly into those repo paths. Do not write into /tmp or any other temp directory.
Do not modify any other files. Do not run tests. Respond with exactly: ok
